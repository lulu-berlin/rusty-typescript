language: node_js

node_js:
  - "10"

before_install:
  - export PATH="$HOME/.cargo/bin:$PATH"
  - curl https://sh.rustup.rs -sSf | sh -s -- -y

cache:
  directories:
    - $TRAVIS_BUILD_DIR/target
    - $HOME/.cargo
    - $HOME/.npm


